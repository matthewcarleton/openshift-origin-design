# Workflows moved to repo root

GitHub Actions only runs workflows under **`.github/workflows/`** at the repository root (`openshift-origin-design/`).

Standalone HPUX Prototypes deploy: **[`.github/workflows/hpux-prototypes-github-pages.yml`](../../../.github/workflows/hpux-prototypes-github-pages.yml)**.
