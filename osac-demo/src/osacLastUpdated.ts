/** Replaced by Vite `define` at dev-server start and at `vite build` (see `vite.config.ts`). */
declare const __OSAC_LANDING_LAST_UPDATED__: string

/** Footer “Last updated” label — matches the date of the deployed GitHub Pages build. */
export const OSAC_LANDING_LAST_UPDATED = __OSAC_LANDING_LAST_UPDATED__
